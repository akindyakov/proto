#include "parser.h"

#include <tools/tests/ut.h>

#include <iostream>


int main() {
    try {
        std::cerr << "parser_ut:\n";
        std::cerr << " - TODO\n";
        std::cerr << std::endl;
    } catch (const std::exception& except) {
        std::cerr << "failed: " << except.what() << std::endl;
        return 1;
    }
    return 0;
}

