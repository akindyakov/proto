#include "creative.h"

#include <lib/tests/assert.h>

#include <iostream>


int main(int argn, char** argv) {
    try {
        std::cerr << "creative_ut:\n";
        std::cerr << " - TODO\n";
        std::cerr << std::endl;
    } catch (const std::exception& except) {
        std::cerr << except.what() << std::endl;
        return 1;
    }
    return 0;
}
